CREATE TABLE sys.loginUser (
    ID INT PRIMARY KEY AUTO_INCREMENT,
    USERID INT ,
    USERNAME VARCHAR(50)
);


INSERT INTO sys.loginUser (USERID, USERNAME) VALUES (1, 'admin');


CREATE TABLE sys.loginStatus(
    ID INT PRIMARY KEY AUTO_INCREMENT,
    RESOURCENAME VARCHAR(50) ,
    USERID VARCHAR(50) ,
    RESOURCEURL VARCHAR(50)
);

INSERT INTO sys.loginstatus (RESOURCENAME, USERID,RESOURCEURL) VALUES ('ReservationForm', '1,2,3','/reservation');
